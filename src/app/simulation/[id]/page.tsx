// import { SimulationPage } from '@/components/transaction-page';

export const runtime = 'edge';

export default async function Page({ params }: { params: { id: string } }) {
	// return <SimulationPage simulationId={params.id} />;
	return <></>;
}
